# energy-valuation-model
Python/SQL data pipeline, custom factor screener (ROIC/Green CapEx), and SOTP valuation model for global energy equities.

# goal
Determine whether we can use a fully renwewable + nuclear portfolio in Asset Management

